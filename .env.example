# --- Firebase Admin SDK credentials ---
# This should be the path to your service account JSON file.
# On Render, you'll upload the JSON content as a secret file and set this path accordingly.
FIREBASE_CRED_PATH=serviceAccountKey.json

# --- Gemini / Google Generative AI ---
# Your Gemini API Key (get from Google AI Studio)
GEMINI_API_KEY=your_gemini_api_key_here

# Model name (optional)
GEN_MODEL=gemini-1.5-pro

# --- Firestore / Firebase Database ---
# This is already hardcoded in your main.py, so only if you plan to make it dynamic:
# FIREBASE_DB_URL=https://bloomwatch-70da0-default-rtdb.firebaseio.com/

# --- Environment / Uvicorn ---
PORT=8000
HOST=0.0.0.0
