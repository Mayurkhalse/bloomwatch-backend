services:
  - type: web
    name: bloomwatch-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: FIREBASE_CRED_PATH
        value: bloomwatch-70da0-firebase-adminsdk-fbsvc-042a03c59e.json
